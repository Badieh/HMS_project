import 'package:flutter/material.dart';

class DoctorFinishedAppointmentScreen extends StatelessWidget {
  const DoctorFinishedAppointmentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
